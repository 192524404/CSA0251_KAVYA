#include <stdio.h>
int main() {
    int n, digit,sum=0;
    printf("Enter an integer: ");
    scanf("%d", &n);
    while (n != 0) {
        digit = n % 10;          
        sum = sum+digit;
        n /= 10;    
    }
    printf("sum of digits: %d\n",sum);
    return 0;
}
