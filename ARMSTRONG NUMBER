#include <stdio.h>
int main() {
    int n, r, sum = 0, t;
    scanf("%d", &n);
    t = n;
    while (t > 0) {
        r = t % 10;
        sum += r * r * r;
        t /= 10;
    }
    printf(n == sum ? "Armstrong" : "Not");
    return 0;
}
